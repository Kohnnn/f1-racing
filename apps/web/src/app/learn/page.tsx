import { LearnIndex } from "./learn-index";

export const metadata = {
  title: "Learn",
  description: "Engineering modules covering the car, aero, tyres, braking, setup, and strategy.",
};

export default function LearnPage() {
  return <LearnIndex />;
}
